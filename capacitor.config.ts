import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'my-vue-project',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
